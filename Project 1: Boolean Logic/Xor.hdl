CHIP Xor {
    IN a, b;
    OUT out;

    PARTS:
    Not(in=a, out=notA );
    Not(in=b, out=notB);
    And(a=notA, b=b, out=outb);
    And(a=a, b=notB, out=outa);
    Or(a=outa, b=outb, out=out);
}